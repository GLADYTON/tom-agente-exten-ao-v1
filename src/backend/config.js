// Backend público. Definir antes de publicar produção.
export const BACKEND_BASE_URL = '';
export const BACKEND_CONFIG = {
  syncIntervalMs: 15 * 60 * 1000,
  offlineGraceMs: 30 * 60 * 1000,
  requestTimeoutMs: 15000,
};
